-- ===== 占位迁移：V2 仅作编号占位，不再插入任何种子账号 =====
-- 账号来源（按产品规则）：
--   管理员  由初始化端点 POST /init/admin 创建（系统无用户时可调用，见 InitAppService）
--   商家    由管理员通过 POST /admin/users 创建
--   旅客    通过 POST /register 自助注册
-- 删除理由：原三个演示账号（user001/merch01/admin01）违背"管理员初始化创建 / 商家管理员分配 / 用户自助注册"的账号规则，
-- 且用户明确要删库重建验证初始化流程。若本迁移曾在旧库执行过，删库重建后该注释迁移会被 Flyway 忽略（版本不重复执行）。
