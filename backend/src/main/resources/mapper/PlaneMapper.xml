<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ronnie.airTicket.infrastructure.mapper.PlaneMapper">

    <select id="list" resultType="PlanePO">
        SELECT id, id_airline, model_name, length, wingspan, height, max_takeoff_weight_kg,
               max_landing_weight_kg, max_seat_first_class, max_seat_business_class,
               max_seat_economy_class, create_at
        FROM plane ORDER BY id
    </select>

    <select id="findById" resultType="PlanePO">
        SELECT id, id_airline, model_name, length, wingspan, height, max_takeoff_weight_kg,
               max_landing_weight_kg, max_seat_first_class, max_seat_business_class,
               max_seat_economy_class, create_at
        FROM plane WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO plane (id_airline, model_name, length, wingspan, height, max_takeoff_weight_kg,
                           max_landing_weight_kg, max_seat_first_class, max_seat_business_class,
                           max_seat_economy_class)
        VALUES (#{idAirline}, #{modelName}, #{length}, #{wingspan}, #{height}, #{maxTakeoffWeightKg},
                #{maxLandingWeightKg}, #{maxSeatFirstClass}, #{maxSeatBusinessClass},
                #{maxSeatEconomyClass})
    </insert>

    <update id="update">
        UPDATE plane
        SET id_airline = #{idAirline},
            model_name = #{modelName},
            length = #{length},
            wingspan = #{wingspan},
            height = #{height},
            max_takeoff_weight_kg = #{maxTakeoffWeightKg},
            max_landing_weight_kg = #{maxLandingWeightKg},
            max_seat_first_class = #{maxSeatFirstClass},
            max_seat_business_class = #{maxSeatBusinessClass},
            max_seat_economy_class = #{maxSeatEconomyClass}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM plane WHERE id = #{id}
    </delete>

    <!-- 删除前引用保护：该机型被多少航班引用 -->
    <select id="countByPlaneId" resultType="long">
        SELECT COUNT(*) FROM flight WHERE id_plane = #{planeId}
    </select>

</mapper>
