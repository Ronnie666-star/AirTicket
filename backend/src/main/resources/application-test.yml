# ===== 本地测试环境：IDEA 直接启动（默认 profile=test） =====
# 数据库：同一 mysql 容器（compose 里只起 mysql 服务），宿主端口 13306 -> 容器 3306
spring:
  datasource:
    url: jdbc:mysql://localhost:13306/flights?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
